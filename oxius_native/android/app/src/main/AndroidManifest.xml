<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Internet permission - REQUIRED for API calls -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Advertising ID - REQUIRED by AdMob so the ad identifier isn't zeroed
         out (Play Console warns when the declaration says the app uses it but
         the merged manifest lacks the permission). -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- NOTE: ACCESS_BACKGROUND_LOCATION intentionally NOT declared. Google
         Play rejected the app over its prominent-disclosure policy for that
         permission. Driver tracking instead runs a foreground service with
         foregroundServiceType="location" (see below), which keeps streaming
         location with the screen off using only the while-in-use permission
         — so the background permission (and its policy review) is unneeded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    
    <!-- FCM Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Allow drivers to exempt the app from Doze/battery optimization so
         ride-request pushes can reliably wake the device when locked. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <!-- CallKit incoming call permissions -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
    <uses-permission android:name="android.permission.USE_SIP"/>
    <uses-permission android:name="android.permission.BIND_TELECOM_CONNECTION_SERVICE"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.TURN_SCREEN_ON"/>

    <!-- Android 14: foreground service accessing microphone (Agora audio calls) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- Android 14: foreground service accessing camera (Agora video calls) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    
    <!-- Permissions for image picker -->
    <uses-permission android:name="android.permission.CAMERA"/>
    
    <!-- Remove media permissions - app uses system photo picker instead -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    
    <application
        android:label="AdsyClub"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowNativeHeapPointerTagging="false">
        
        <!-- Support for 16 KB memory page sizes -->
        <property android:name="android.app.16kb_page_size" android:value="true"/>
        
        <!-- UCrop Activity for image cropping -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <!-- Disable Flutter's built-in automatic deep linking.
                 Deep links are handled exclusively by the app_links package
                 (DeepLinkService). Leaving Flutter's default deep linking
                 enabled causes every App Link intent to ALSO be pushed onto
                 the Navigator as a raw named route (e.g.
                 "/details/<slug>?fbclid=..."), which renders the
                 "Page not found" screen on top of the correct page.
                 IMPORTANT: this flag MUST live inside <activity>. Flutter's
                 Android embedding reads it from the Activity's metadata
                 (FlutterActivity.shouldHandleDeeplinking); placed at the
                 <application> level it is silently ignored, which is why
                 Android still showed "Page not found" while iOS (Info.plist
                 FlutterDeepLinkingEnabled, which is global) worked. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- App Links: claim ONLY the specific paths that have an in-app
                 screen (matches the iOS apple-app-site-association). Pages with
                 no app screen — e.g. /donate — are intentionally NOT listed, so
                 they open in the browser instead of (wrongly) launching the app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/business-network"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/business-network"/>
                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/food-zone"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/food-zone"/>
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/classified-details"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/classified-details"/>
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/sale"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/sale"/>
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/deposit-withdraw"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/deposit-withdraw"/>
                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/verify-payment"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/verify-payment"/>
                <data android:scheme="https" android:host="adsyclub.com" android:pathPrefix="/payment-callback.html"/>
                <data android:scheme="https" android:host="www.adsyclub.com" android:pathPrefix="/payment-callback.html"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="adsyclub"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Google AdMob application ID (AdsyClub Android) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7353770814470592~7885293673" />
        
        <!-- Default notification channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="oxius_messages" />
        
        <!-- Notification icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        
        <!-- Notification color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@android:color/holo_green_light" />

        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location"
            tools:node="merge" />

        <!-- Facebook Login configuration. The actual values live in
             res/values/strings.xml (facebook_app_id / facebook_client_token /
             fb_login_protocol_scheme) and must be filled with your Facebook
             app's credentials before release. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
